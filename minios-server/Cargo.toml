[package]
name = "minios-server"
version = "0.1.0"
edition = "2021"
description = "minios object storage server daemon"

[dependencies]
minios-lib = { path = "../minios-lib" }
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
libc = "0.2"
