[package]
name = "minios-client"
version = "0.1.0"
edition = "2021"
description = "minios object storage client CLI"

[dependencies]
minios-lib = { path = "../minios-lib" }
clap = { version = "4", features = ["derive"] }
libc = "0.2"
