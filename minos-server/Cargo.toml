[package]
name = "minos-server"
version = "0.1.0"
edition = "2021"
description = "MinOS object storage server daemon"

[dependencies]
minos-lib = { path = "../minos-lib" }
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
libc = "0.2"
