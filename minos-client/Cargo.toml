[package]
name = "minos-client"
version = "0.1.0"
edition = "2021"
description = "MinOS object storage client CLI"

[dependencies]
minos-lib = { path = "../minos-lib" }
clap = { version = "4", features = ["derive"] }
libc = "0.2"
